/*
 * Authors: JavaRush's team, CodeGym's team, Altavista360
 * Version: 1.0.0
 */

/*
 * // JavaRush
 * Задача: Настройка датчиков метеостанции ☀️
 * Ссылка: https://javarush.com/tasks/ru.javarush.java.core.level07.task03
 */

/*
 * // CodeGym
 * Task: Configuring the weather station sensors ☀️
 * Link: https://codegym.cc/tasks/en.codegym.java.core.level07.task03
 */

/*
 * // JavaRush
 * Условие:
 * Ваша новая метеостанция готова к работе, и она должна записывать четыре ключевых измерения атмосферы. Изначально некоторые показания могут быть нулевыми, но вы только что получили критическое обновление для третьего датчика (который, конечно же, для программистов обозначается индексом 2!). Вам необходимо вручную ввести новое значение атмосферного давления, 3.14, для этого конкретного датчика. После этого ручного обновления очень важно просмотреть все показания датчиков, чтобы убедиться, что они корректно считываются.
 * Создайте массив double[] sensorReadings для четырех показаний. Затем, в ячейку с индексом 2 (то есть, третью по счету) запишите значение 3.14. После этого выведите на экран значения всех ячеек массива, разделяя их пробелом в одной строке, чтобы проверить актуальное состояние всех датчиков.
 */

/*
 * // JavaRush
 * Требования:
 * • Необходимо создать массив типа double длиной 4 элемента с именем sensorReadings.
 * • В элемент массива sensorReadings с индексом 2 должно быть записано значение 3.14.
 * • После внесения изменений требуется вывести на экран все значения массива sensorReadings в одной строке, разделяя их пробелом.
 * • Порядок вывода должен соответствовать порядку элементов в массиве (от индекса 0 до 3).
 */

/*
 * // JavaRush
 * Черновик:
 * package ru.javarush.java.core.level07.task03;
 *
 * public class Solution {
 *     public static void main(String[] args) {
 *         // Объявляем массив для хранения четырёх показаний датчиков
 *
 *
 *         // В элемент с индексом 2 (третий по счёту) записываем значение 3.14
 *
 *
 *         // Выводим все значения массива в одну строку, разделяя их пробелом
 *
 *
 *         // После каждого элемента, кроме последнего, выводим пробел
 *
 *
 *     }
 * }
 */

// JavaRush
package ru.javarush.java.core.level07.task03;

public class Solution {
    public static void main(String[] args) {
        // Объявляем массив для хранения четырёх показаний датчиков
        double[] sensorReadings = new double[4];

        // В элемент с индексом 2 (третий по счёту) записываем значение 3.14
        sensorReadings[2] = 3.14;

        // Выводим все значения массива в одну строку, разделяя их пробелом
        for (int i = 0; i < sensorReadings.length; i++) {
            System.out.print(sensorReadings[i]);

        // После каждого элемента, кроме последнего, выводим пробел
            if (i < sensorReadings.length - 1) {
            System.out.print(" ");
            }
        }
    }
}

/*
 * // JavaRush
 * Правильное решение:
 * // Author: JavaRush's team
 * package ru.javarush.java.core.level07.task03;
 *
 * public class Solution {
 *     public static void main(String[] args) {
 *         // Объявляем массив для хранения четырёх показаний датчиков
 *         double[] sensorReadings = new double[4];
 *
 *         // В элемент с индексом 2 (третий по счёту) записываем значение 3.14
 *         sensorReadings[2] = 3.14;
 *
 *         // Выводим все значения массива в одну строку, разделяя их пробелом
 *         for (int i = 0; i < sensorReadings.length; i++) {
 *             System.out.print(sensorReadings[i]);
 *             // После каждого элемента, кроме последнего, выводим пробел
 *             if (i < sensorReadings.length - 1) {
 *                 System.out.print(" ");
 *             }
 *         }
 *     }
 * }
 */