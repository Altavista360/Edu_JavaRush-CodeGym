/*
 * Authors: JavaRush's team, CodeGym's team, Altavista360
 * Version: 1.0.0
 */

/*
 * // JavaRush
 * Задача: Самая жаркая точка в здании по показаниям датчиков 🔥
 * Ссылка: https://javarush.com/tasks/ru.javarush.java.core.level07.task16
 */

/*
 * // CodeGym
 * Task: The hottest spot in the building according to sensor readings 🔥
 * Link: https://codegym.cc/tasks/en.codegym.java.core.level07.task16
 */

/*
 * // JavaRush
 * Условие:
 * Вы отслеживаете температуру в трех различных климатических зонах умного здания, но в каждой зоне разное количество датчиков. Например, в Зоне A — три датчика, в Зоне B — два, а в Зоне C — четыре. Вы собрали показания температуры со всех этих датчиков (выберите любые целые значения для показаний). Ваша миссия — найти самое высокое одиночное значение температуры, зафиксированное во всех зонах, чтобы определить самую жаркую точку в здании.
 * Создайте зубчатый массив int[][] zoneTemperatures, где первая строка содержит три числа, вторая — два числа, третья — четыре числа (значения выберите сами, например, 22, 25, 20 и т.д.). Найдите максимальное значение среди всех элементов этого массива и выведите его на экран, чтобы определить самую высокую температуру.
 */

/*
 * // JavaRush
 * Требования:
 * • В программе должен быть создан публичный класс с именем Program.
 * • В классе Program должен быть объявлен статический метод main с сигнатурой public static void main(String[] args).
 * • Внутри метода main должно быть выполнено отображение строки "Привет, Java!" на экран.
 * • Программа должна вывести на экран только строку "Привет, Java!" без дополнительных символов, пробелов или строк.
 */

/*
 * // JavaRush
 * Черновик:
 * package ru.javarush.java.core.level07.task16;
 *
 * public class Solution {
 *     public static void main(String[] args) {
 *         // Создаем зубчатый массив с заданным количеством элементов в каждой строке
 *         // Зона A: три датчика
 *         // Зона B: два датчика
 *         // Зона C: четыре датчика
 *
 *         // Инициализируем переменную для хранения максимального значения
 *
 *
 *         // Проходим по всем элементам зубчатого массива
 *
 *
 *         // Обновляем максимальное значение, если текущая температура больше
 *
 *
 *
 *
 *         // Выводим максимальное значение температуры на экран
 *
 *     }
 * }
 */

// JavaRush
package ru.javarush.java.core.level07.task16;

public class Solution {
    public static void main(String[] args) {
        // Создаем зубчатый массив с заданным количеством элементов в каждой строке
        int[][] zoneTemperatures = {
            {22, 25, 20}, // Зона A: три датчика
            {30, 28}, // Зона B: два датчика
            {18, 24, 27, 29}// Зона C: четыре датчика
        };
        // Инициализируем переменную для хранения максимального значения
        int maxTemp = Integer.MIN_VALUE;

        // Проходим по всем элементам зубчатого массива
        for (int[] zone : zoneTemperatures) {
            for (int temperature : zone) {
        // Обновляем максимальное значение, если текущая температура больше
                if (temperature > maxTemp) {
                    maxTemp = temperature;
                }
            }
        }
        // Выводим максимальное значение температуры на экран
        System.out.println("Максимальная температура: " + maxTemp);
    }
}

/*
 * // JavaRush
 * Правильное решение:
 * // Author: JavaRush's team
 * package ru.javarush.java.core.level07.task16;
 *
 * public class Solution {
 *     public static void main(String[] args) {
 *         // Создаем зубчатый массив с заданным количеством элементов в каждой строке
 *         int[][] zoneTemperatures = {
 *             {22, 25, 20}, // Зона A: три датчика
 *             {30, 28},     // Зона B: два датчика
 *             {18, 24, 27, 29} // Зона C: четыре датчика
 *         };
 *
 *         // Инициализируем переменную для хранения максимального значения
 *         int maxTemperature = Integer.MIN_VALUE;
 *
 *         // Проходим по всем элементам зубчатого массива
 *         for (int[] zone : zoneTemperatures) {
 *             for (int temperature : zone) {
 *                 // Обновляем максимальное значение, если текущая температура больше
 *                 if (temperature > maxTemperature) {
 *                     maxTemperature = temperature;
 *                 }
 *             }
 *         }
 *
 *         // Выводим максимальное значение температуры на экран
 *         System.out.println("Самая высокая температура в здании: " + maxTemperature);
 *     }
 * }
 */