/*
 * Authors: JavaRush's team, CodeGym's team, Altavista360
 * Version: 1.0.0
 */

/*
 * // JavaRush
 * Задача: Разгадываем последовательность запуска системы 🧩
 * Ссылка: https://javarush.com/tasks/ru.javarush.java.core.level15.task19
 */

/*
 * // CodeGym
 * Task: Unraveling the system startup sequence 🧩
 * Link: https://codegym.cc/tasks/en.codegym.java.core.level15.task19
 */

/*
 * // JavaRush
 * Условие:
 * Представьте, что вы проводите аудит сложной системы и вам крайне важно понять точный порядок, в котором инициализируются её компоненты. Где-то есть глобальные настройки, где-то — общие для каждого нового экземпляра, а где-то — индивидуальная настройка при создании.
 * Создайте класс SystemComponent. Внутри этого класса определите следующие элементы, и в каждом из них добавьте уникальное сообщение для вывода на экран, чтобы чётко отслеживать порядок выполнения:
 * • Первый статический блок, выводящий "Глобальная инициализация: Шаг 1".
 * • Второй статический блок, выводящий "Глобальная инициализация: Шаг 2".
 * • Первый нестатический блок инициализации (инстанс-блок), выводящий "Инициализация компонента: Шаг А".
 * • Второй нестатический блок инициализации (инстанс-блок), выводящий "Инициализация компонента: Шаг Б".
 * • Публичный конструктор без параметров, выводящий "Компонент полностью настроен и готов!".
 * В классе Solution создайте два объекта класса SystemComponent. Запустите программу и внимательно изучите последовательность вывода сообщений. Вы должны увидеть, что статические блоки выполняются один раз при первой загрузке класса, а затем для каждого создаваемого объекта сначала срабатывают нестатические блоки (в порядке их определения), и только потом — конструктор.
 */

/*
 * // JavaRush
 * Требования:
 * • В задаче должен быть создан класс с именем SystemComponent.
 * • В классе SystemComponent должны быть определены два статических блока инициализации, каждый из которых выводит своё уникальное сообщение: "Глобальная инициализация: Шаг 1" и "Глобальная инициализация: Шаг 2".
 * • Первый статический блок должен выводить сообщение "Глобальная инициализация: Шаг 1" до второго, который выводит "Глобальная инициализация: Шаг 2".
 * • В классе SystemComponent должны быть определены два нестатических (инстанс-) блока инициализации, каждый из которых выводит своё уникальное сообщение: "Инициализация компонента: Шаг А" и "Инициализация компонента: Шаг Б".
 * • Первый нестатический блок должен выводить сообщение "Инициализация компонента: Шаг А" до второго, который выводит "Инициализация компонента: Шаг Б".
 * • В классе SystemComponent должен быть определён публичный конструктор без параметров, который выводит сообщение "Компонент полностью настроен и готов!".
 * • В классе Solution должно быть создано два объекта класса SystemComponent.
 * • Статические блоки должны выполняться только один раз при первой загрузке класса, а для каждого создаваемого объекта должны последовательно выполняться оба нестатических блока (в порядке их определения), а затем конструктор.
 */

// JavaRush
package ru.javarush.java.core.level15.task19;

public class SystemComponent {

    static {
        System.out.println("Глобальная инициализация: Шаг 1");
    }

    static {
        System.out.println("Глобальная инициализация: Шаг 2");
    }

    {
        System.out.println("Инициализация компонента: Шаг А");
    }

    {
        System.out.println("Инициализация компонента: Шаг Б");
    }

    public SystemComponent() {
        System.out.println("Компонент полностью настроен и готов!");
    }
}

/*
 * // JavaRush
 * Правильное решение:
 * // Author: JavaRush's team
 * package ru.javarush.java.core.level15.task19;
 *
 * public class SystemComponent {
 *
 *     static {
 *         // Первый статический блок: выполнится один раз при загрузке класса
 *         System.out.println("Глобальная инициализация: Шаг 1");
 *     }
 *
 *     static {
 *         // Второй статический блок: выполнится сразу после первого, тоже один раз
 *         System.out.println("Глобальная инициализация: Шаг 2");
 *     }
 *
 *     {
 *         // Первый нестатический (инстанс-) блок: выполняется перед конструктором для каждого объекта
 *         System.out.println("Инициализация компонента: Шаг А");
 *     }
 *
 *     {
 *         // Второй нестатический (инстанс-) блок: выполняется после первого блока, перед конструктором
 *         System.out.println("Инициализация компонента: Шаг Б");
 *     }
 *
 *     public SystemComponent() {
 *         // Конструктор: завершающий шаг для каждого создаваемого объекта
 *         System.out.println("Компонент полностью настроен и готов!");
 *     }
 * }
 */