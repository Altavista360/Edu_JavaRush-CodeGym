/*
 * Authors: JavaRush's team, CodeGym's team, Altavista360
 * Version: 1.0.0
 */

/*
 * // JavaRush
 * Задача: Подсчет общей стоимости покупок 🛍️
 * Ссылка: https://javarush.com/tasks/ru.javarush.java.core.level04.task07
 */

/*
 * // CodeGym
 * Task: Calculating the total cost of purchases 🛍️
 * Link: https://codegym.cc/tasks/en.codegym.java.core.level04.task07
 */

/*
 * // JavaRush
 * Условие:
 * Представьте, что у вас есть список из N товаров. Пусть переменная N содержит количество товаров, например, 7.
 * Вам нужно посчитать общую стоимость, если цена каждого товара равна его порядковому номеру (первый — 1, второй — 2 и так далее).
 * Создайте цикл for, который вычислит и выведет общую сумму.
 */

/*
 * // JavaRush
 * Требования:
 * • В программе должна быть переменная N, которая хранит количество товаров.
 * • Для подсчета суммы должен использоваться цикл for.
 * • Цикл for должен проходить по всем товарам от 1 до N включительно.
 * • На каждой итерации к общей сумме должна добавляться текущая стоимость товара, равная его порядковому номеру.
 * • После завершения цикла программа должна вывести общую стоимость всех товаров.
 */

/*
 * // JavaRush
 * Черновик:
 * package ru.javarush.java.core.level04.task07;
 *
 * public class Solution {
 *     public static void main(String[] args) {
 *         // Объявляем переменную N для хранения количества товаров
 *
 *
 *         // Переменная для хранения общей стоимости
 *
 *
 *         // Используем цикл for для подсчета общей стоимости товаров
 *         for (/* инициализация */; /* условие */; /* изменение */) {
 *             // Добавляем стоимость текущего товара (равную его порядковому номеру)
 *
 *         }
 *
 *         // Выводим общую стоимость всех товаров
 *
 *     }
 * }
 */

// JavaRush
package ru.javarush.java.core.level04.task07;

public class Solution {
    public static void main(String[] args) {
        // Объявляем переменную N для хранения количества товаров
        int N = 7;

        // Переменная для хранения общей стоимости
        int totalSum = 0;

        // Используем цикл for для подсчета общей стоимости товаров
        for (int i = 1; i <= N; i++) {
            totalSum += i; // Добавляем стоимость текущего товара (равную его порядковому номеру)
        }

        // Выводим общую стоимость всех товаров
        System.out.println("Итого: " + totalSum);
    }
}

/*
 * // JavaRush
 * Правильное решение:
 * // Author: JavaRush's team
 * package ru.javarush.java.core.level04.task07;
 *
 * public class Solution {
 *     public static void main(String[] args) {
 *         // Объявляем переменную N для хранения количества товаров
 *         int N = 7;
 *
 *         // Переменная для хранения общей стоимости
 *         int totalCost = 0;
 *
 *         // Используем цикл for для подсчета общей стоимости товаров
 *         for (int i = 1; i <= N; i++) {
 *             totalCost += i; // Добавляем стоимость текущего товара (равную его порядковому номеру)
 *         }
 *
 *         // Выводим общую стоимость всех товаров
 *         System.out.println("Общая стоимость всех товаров: " + totalCost);
 *     }
 * }
 */