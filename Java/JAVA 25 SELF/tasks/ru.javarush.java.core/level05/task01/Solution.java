/*
 * Authors: JavaRush's team, CodeGym's team, Altavista360
 * Version: 1.0.0
 */

/*
 * // JavaRush
 * Задача: Лестница для цифрового замка 🏰
 * Ссылка: https://javarush.com/tasks/ru.javarush.java.core.level05.task01
 */

/*
 * // CodeGym
 * Task: Staircase for the Digital Castle 🏰
 * Link: https://codegym.cc/tasks/en.codegym.java.core.level05.task01
 */

/*
 * // JavaRush
 * Условие:
 * Представьте, что вы архитектор цифровых строений, и ваша задача — возвести крепкую, красивую лестницу для входа в ваш виртуальный замок. Каждая ступенька этой лестницы будет символом #, и чем выше ступенька, тем больше символов она должна содержать.
 * Вам нужно попросить пользователя указать высоту этой лестницы, то есть количество ступенек, которые она должна иметь (например, ladderHeight). Затем ваша программа должна тщательно "построить" эту лестницу: на первой строке появится одна решетка, на второй — две, на третьей — три, и так далее, пока не будет достигнута заданная высота N.
 * Пример, как будет выглядеть ваша цифровая лестница, если пользователь введет число 4:
 * #
 * ##
 * ###
 * ####
 */

/*
 * // JavaRush
 * Требования:
 * • Программа должна запросить у пользователя ввод числа — высоты лестницы (ladderHeight).
 * • Для построения каждой ступеньки лестницы должна использоваться конструкция цикла.
 * • На каждой строке количество символов '#' должно увеличиваться на один по сравнению с предыдущей строкой, начиная с одной решётки.
 * • Каждая ступенька (строка с решётками) должна выводиться на отдельной строке.
 * • Введённое пользователем значение должно быть использовано для определения количества строк (ступеней) в лестнице.
 */

/*
 * // JavaRush
 * Черновик:
 * package ru.javarush.java.core.level05.task01;
 *
 * import java.util.Scanner;
 *
 * public class Solution {
 *     public static void main(String[] args) {
 *         // Создаем объект Scanner для чтения ввода пользователя
 *
 *
 *         // Запрашиваем у пользователя высоту лестницы
 *
 *
 *         // Внешний цикл — по количеству ступеней (строк)
 *         for (/* инициализация */; /* условие */; /* изменение */) {
 *             // Внутренний цикл — печатает нужное количество символов '#'
 *             for (/* инициализация */; /* условие */; /* изменение */) {
 *
 *             }
 *             // Переход на новую строку после каждой ступеньки
 *
 *         }
 *     }
 * }
 */

// JavaRush
package ru.javarush.java.core.level05.task01;

import java.util.Scanner;

public class Solution {
    public static void main(String[] args) {
        // Создаем объект Scanner для чтения ввода пользователя
        Scanner inputData = new Scanner(System.in);

        // Запрашиваем у пользователя высоту лестницы
        System.out.print("Введите высоту лестницы: ");
        int ladderHeight = inputData.nextInt();

        // Внешний цикл — по количеству ступеней (строк)
        for ( i = 1/* инициализация */; i <= ladderHeight/* условие */; i++/* изменение */) {
            // Внутренний цикл — печатает нужное количество символов '#'
            for (int j = 0/* инициализация */; j < i/* условие */; j++/* изменение */) {
                System.out.print("#");
            }
            System.out.println();// Переход на новую строку после каждой ступеньки

        }
    }
}

/*
 * // JavaRush
 * Правильное решение:
 * // Author: JavaRush's team
 * package ru.javarush.java.core.level05.task01;
 *
 * import java.util.Scanner;
 *
 * public class Solution {
 *     public static void main(String[] args) {
 *         // Создаем объект Scanner для чтения ввода пользователя
 *         Scanner console = new Scanner(System.in);
 *
 *         // Запрашиваем у пользователя высоту лестницы
 *         System.out.print("Введите высоту лестницы: ");
 *         int ladderHeight = console.nextInt();
 *
 *         // Внешний цикл — по количеству ступеней (строк)
 *         for (int step = 1; step <= ladderHeight; step++) {
 *             // Внутренний цикл — печатает нужное количество символов '#'
 *             for (int i = 0; i < step; i++) {
 *                 System.out.print("#");
 *             }
 *             // Переход на новую строку после каждой ступеньки
 *             System.out.println();
 *         }
 *     }
 * }
 */