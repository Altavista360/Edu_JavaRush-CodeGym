/*
 * Authors: JavaRush's team, CodeGym's team, Altavista360
 * Version: 1.0.0
 */

/*
 * // JavaRush
 * Задача: Критический модуль и аварийное завершение с отчетом 💥
 * Ссылка: https://javarush.com/tasks/ru.javarush.java.core.level11.task11
 */

/*
 * // CodeGym
 * Task: Critical module and emergency termination with report 💥
 * Link: https://codegym.cc/tasks/en.codegym.java.core.level11.task11
 */

/*
 * // JavaRush
 * Условие:
 * Вы разрабатываете критически важный модуль, который должен всегда выполнять определённые финальные действия (например, освободить занятый ресурс), даже если происходит фатальная ошибка, которая не будет перехвачена на этом уровне.
 * В блоке try намеренно вызовите ошибку, разделив 5 на 0.
 * Важно: не используйте блок catch для этого исключения. Вместо этого, создайте блок finally и внутри него выведите сообщение: "Датчик сообщает: Работа finally завершена. Даже в случае аварии."
 * Запустите программу и убедитесь, что, несмотря на то, что программа завершится с ошибкой (красным сообщением в консоли), ваше сообщение из finally будет выведено перед этим.
 */

/*
 * // JavaRush
 * Требования:
 * • В программе должен использоваться блок try, за которым сразу следует блок finally, без использования блока catch.
 * • Внутри блока try должно выполняться деление числа 5 на 0, чтобы намеренно вызвать арифметическую ошибку (ArithmeticException).
 * • В блоке finally должно быть реализовано действие, которое выполняется независимо от возникновения исключения.
 * • В блоке finally должно выводиться сообщение: "Датчик сообщает: Работа finally завершена. Даже в случае аварии."
 * • Программа должна завершаться с ошибкой (вывод красного сообщения об исключении в консоль), но сообщение из блока finally должно быть напечатано перед этим.
 */

/*
 * // JavaRush
 * Черновик:
 * package ru.javarush.java.core.level11.task11;
 *
 * public class Solution {
 *     public static void main(String[] args) {
 *         // Намеренно вызываем арифметическую ошибку, деля 5 на 0
 *
 *
 *         // Блок finally выполняется в любом случае, даже если произошло исключение
 *
 *     }
 * }
 */

// JavaRush
package ru.javarush.java.core.level11.task11;

public class Solution {
    public static void main(String[] args) {
        try {
            // Намеренно вызываем арифметическую ошибку, деля 5 на 0
            int foo = 5 / 0;
        } finally {
            // Блок finally выполняется в любом случае, даже если произошло исключение
            System.out.println("Датчик сообщает: Работа finally завершена. Даже в случае аварии.");
        }
    }
}

/*
 * // JavaRush
 * Правильное решение:
 * // Author: JavaRush's team
 * package ru.javarush.java.core.level11.task11;
 *
 * public class Solution {
 *     public static void main(String[] args) {
 *         try {
 *             // Намеренно вызываем арифметическую ошибку, деля 5 на 0
 *             int result = 5 / 0;
 *         } finally {
 *             // Блок finally выполняется в любом случае, даже если произошло исключение
 *             System.out.println("Датчик сообщает: Работа finally завершена. Даже в случае аварии.");
 *         }
 *     }
 * }
 */