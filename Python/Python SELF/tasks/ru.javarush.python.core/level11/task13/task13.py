## Authors: JavaRush's team, CodeGym's team, Altavista360
## Version: 1.0.0

### JavaRush
## Задача: Параметры по умолчанию.
## Ссылка: https://javarush.com/tasks/ru.javarush.python.core.level11.task13

### CodeGym
## Task: Default parameters.
## Link: https://codegym.cc/tasks/en.codegym.python.core.level11.task13

### JavaRush
## Условие:
# Напишите 2 функции: def foo(bar=[]) и foo_correct(bar=None). Каждая функция принимает список и добавляет в него элемент "baz". Если список не передан, функция должна использовать новый пустой список. Покажите, как использование изменяемого объекта в качестве значения по умолчанию может привести к неожиданному поведению, и исправьте это (во второй функции).

### JavaRush
## Требования:
# • Программа должна включать функцию foo, которая принимает один параметр bar со значением по умолчанию [], добавляет в него элемент "baz", и возвращает измененный список.
# • Программа должна включать функцию foo_correct, которая принимает один параметр bar со значением по умолчанию None, добавляет в него элемент "baz", и возвращает измененный список.
# • Программа должна продемонстрировать, как использование изменяемого объекта (списка) в качестве значения по умолчанию приводит к накоплению элементов при многократных вызовах функции foo без передачи параметров.
# • Программа должна показать, как использование значения по умолчанию None и создание нового списка внутри функции foo_correct устраняет проблему накопления элементов при многократных вызовах функции без передачи параметров.

### JavaRush
## Черновик:
# # Параметры по умолчанию.
#
# # Напишите 2 функции: def foo(bar=[]) и foo_correct(bar=None).
# # Каждая функция принимает список и добавляет в него элемент "baz".
# # Если список не передан, функция должна использовать новый пустой список.
# # Покажите, как использование изменяемого объекта в качестве значения по умолчанию может привести к неожиданному поведению, и исправьте это (во второй функции).
#
#
# # Напишите тут ваш код
#
# # Проверка исправленной функции
# print(foo_correct())  # Выводит: ['baz']
# print(foo_correct())  # Выводит: ['baz']
# print(foo_correct())  # Выводит: ['baz']
#
# # Проверка с передачей списка
# lst = [1, 2, 3]
# print(foo_correct(lst))  # Выводит: [1, 2, 3, "baz"]
# print(foo_correct(lst))  # Выводит: [1, 2, 3, "baz", "baz"]

### JavaRush
# Параметры по умолчанию.

# Напишите 2 функции: def foo(bar=[]) и foo_correct(bar=None).
# Каждая функция принимает список и добавляет в него элемент "baz".
# Если список не передан, функция должна использовать новый пустой список.
# Покажите, как использование изменяемого объекта в качестве значения по умолчанию может привести к неожиданному поведению, и исправьте это (во второй функции).


# Напишите тут ваш код

def foo(bar=[]):
    bar.append("baz")
    return bar

print(foo())
print(foo())
print(foo())

def foo_correct(bar=None):
    if bar is None:
        bar = []
    bar.append("baz")
    return bar

# Проверка исправленной функции
print(foo_correct())  # Выводит: ['baz']
print(foo_correct())  # Выводит: ['baz']
print(foo_correct())  # Выводит: ['baz']

# Проверка с передачей списка
lst = [1, 2, 3]
print(foo_correct(lst))  # Выводит: [1, 2, 3, "baz"]
print(foo_correct(lst))  # Выводит: [1, 2, 3, "baz", "baz"]

### JavaRush
## Правильное решение:
## Author: JavaRush's team
# # Пример функции с изменяемым объектом в качестве значения по умолчанию
# def foo(bar=[]):
#     bar.append("baz")
#     return bar
#
# # Демонстрация неожиданного поведения
# print(foo())  # Выводит: ['baz']
# print(foo())  # Выводит: ['baz', 'baz']
# print(foo())  # Выводит: ['baz', 'baz', 'baz']
#
# # Исправленная функция с None в качестве значения по умолчанию
# def foo_correct(bar=None):
#     if bar is None:
#         bar = []
#     bar.append("baz")
#     return bar
#
# # Проверка исправленной функции
# print(foo_correct())  # Выводит: ['baz']
# print(foo_correct())  # Выводит: ['baz']
# print(foo_correct())  # Выводит: ['baz']
#
# # Проверка с передачей списка
# lst = [1, 2, 3]
# print(foo_correct(lst))  # Выводит: [1, 2, 3, "baz"]
# print(foo_correct(lst))  # Выводит: [1, 2, 3, "baz", "baz"]